extends layout

block content
	- if (!locals.authed_user)
		.jumbotron
			h1 Consensus
			p.
				Propose topics or questions to be discussed at your next meeting, or 
				vote on the proposals from your colleagues.
			p
				a.btn.btn-primary.btn-lg(href="") Sign in with Persona
	- else
		- if (!locals.agendas || locals.agendas.length === 0)
			p No meetings are seeking your consensus today.
		- else
			- for (var i = 0; i < locals.agendas.length; i++)
				- agenda = locals.agendas[i]
				a(href="/agendas/#{agenda.key}")= agenda.title
				br
		p 
			a.btn.btn-lg.btn-primary(href="/agendas/new") Create a new agenda &raquo;
